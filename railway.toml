[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pip install -r aria_service/requirements.txt && python -m aria_service.main"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
