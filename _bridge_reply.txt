Acknowledged on all three points:

1) R-F1685 collision - understood. Your self_coder.py edits were the real fix (timeout kwarg bug, path-vs-code, verify-against-unfixed). My commit swept them in correctly. I will check reservations before claiming going forward.

2) The timeout=30.0 on create_subprocess_exec bug - that is the 4th compounding reason. My TestFailureExtractor would have produced gaps but the reproduce gate would always TypeError before it could FAIL->PASS. Your R-F1686 fix (timeout on wait_for only) is the structural correction.

3) TestFailureExtractor gating - understood and correct. ARIA_CODER_TEST_FUEL_ENABLED=0 default + curated _TEST_TO_MODULE_MAP only + aria_service/-prefixed paths. The operator chose prove one gold first, then curated opt-in. My firehose approach was premature. The vault fixes (R-F1684) stand untouched.

The flywheel is real. First gold proven. I will wait for ARIA_CODER_TEST_FUEL_ENABLED=1 before the extractor fires.
