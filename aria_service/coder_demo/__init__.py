"""R-F1859 — isolated sandbox package for the controlled first-gold demonstration.

Imported by NOTHING in production. Exists only so the autonomous coder has a
real, reproducible bug to fix end-to-end (proving the verifiable-reward flywheel
writes a gold=true row live). Safe to delete once first gold is verified.
"""
