{
  "standard_version": "1.0.0",
  "axes": {
    "tooluse_adverse": {
      "axis": "tooluse_adverse",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "IS-15": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": null,
      "why": "every fundamental it covers (IS-15) has a deterministic reader, so this is a CANDIDATE for advisory \u2014 but no evidence is recorded that the model's answer fails to reach the customer, and a reader answers the question without silencing the narrative. It keeps blocking until that is measured (R-F4278)"
    },
    "tooluse_challenge": {
      "axis": "tooluse_challenge",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "IS-13": "deterministic"
      },
      "why": "behaviour axis \u2014 it measures the model's own conduct (refusing to rubber-stamp, contradicting a premise from evidence), which no deterministic reader can supply"
    },
    "tooluse_challenge_unavailable": {
      "axis": "tooluse_challenge_unavailable",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "IS-13": "deterministic"
      },
      "why": "behaviour axis \u2014 it measures the model's own conduct (refusing to rubber-stamp, contradicting a premise from evidence), which no deterministic reader can supply"
    },
    "tooluse_charges": {
      "axis": "tooluse_charges",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "FS-12": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": null,
      "why": "every fundamental it covers (FS-12) has a deterministic reader, so this is a CANDIDATE for advisory \u2014 but no evidence is recorded that the model's answer fails to reach the customer, and a reader answers the question without silencing the narrative. It keeps blocking until that is measured (R-F4278)"
    },
    "tooluse_contradiction": {
      "axis": "tooluse_contradiction",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "IS-13": "deterministic",
        "IS-15": "deterministic"
      },
      "why": "behaviour axis \u2014 it measures the model's own conduct (refusing to rubber-stamp, contradicting a premise from evidence), which no deterministic reader can supply"
    },
    "tooluse_insolvency": {
      "axis": "tooluse_insolvency",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "FS-11": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": null,
      "why": "every fundamental it covers (FS-11) has a deterministic reader, so this is a CANDIDATE for advisory \u2014 but no evidence is recorded that the model's answer fails to reach the customer, and a reader answers the question without silencing the narrative. It keeps blocking until that is measured (R-F4278)"
    },
    "tooluse_multihop": {
      "axis": "tooluse_multihop",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "EI-1": "deterministic",
        "OC-6": "deterministic",
        "IS-13b": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": null,
      "why": "every fundamental it covers (EI-1, IS-13b, OC-6) has a deterministic reader, so this is a CANDIDATE for advisory \u2014 but no evidence is recorded that the model's answer fails to reach the customer, and a reader answers the question without silencing the narrative. It keeps blocking until that is measured (R-F4278)"
    },
    "tooluse_news_impact": {
      "axis": "tooluse_news_impact",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "IS-15": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": null,
      "why": "every fundamental it covers (IS-15) has a deterministic reader, so this is a CANDIDATE for advisory \u2014 but no evidence is recorded that the model's answer fails to reach the customer, and a reader answers the question without silencing the narrative. It keeps blocking until that is measured (R-F4278)"
    },
    "tooluse_ownership": {
      "axis": "tooluse_ownership",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "OC-5": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": null,
      "why": "every fundamental it covers (OC-5) has a deterministic reader, so this is a CANDIDATE for advisory \u2014 but no evidence is recorded that the model's answer fails to reach the customer, and a reader answers the question without silencing the narrative. It keeps blocking until that is measured (R-F4278)"
    },
    "tooluse_person": {
      "axis": "tooluse_person",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "IS-13b": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": null,
      "why": "every fundamental it covers (IS-13b) has a deterministic reader, so this is a CANDIDATE for advisory \u2014 but no evidence is recorded that the model's answer fails to reach the customer, and a reader answers the question without silencing the narrative. It keeps blocking until that is measured (R-F4278)"
    },
    "tooluse_resolution": {
      "axis": "tooluse_resolution",
      "mechanism": "deterministic",
      "fundamentals": {
        "EI-1": "deterministic",
        "EI-2": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": "enforce_resolution_response (R-F4144) REPLACES the model's answer when identity is gated, on BOTH the chat and streaming surfaces, and yields a {'type':'replace'} event so already-streamed text is corrected; pinned in CI by test_rf4144. Measured in R-F4257: 0 of 168 eval rows carry the production gate marker and the pod evaluator never imports companies_house.",
      "why": "every fundamental it covers (EI-1, EI-2) has a deterministic reader AND the model's answer is overridden: enforce_resolution_response (R-F4144) REPLACES the model's answer when identity is gated, on BOTH the chat and streaming surfaces, and yields a {'type':'replace'} event so already-streamed text is corrected; pinned in CI by test_rf4144. Measured in R-F4257: 0 of 168 eval rows carry the production gate marker and the pod evaluator never imports companies_house."
    },
    "tooluse_trace": {
      "axis": "tooluse_trace",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "IS-13": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": null,
      "why": "every fundamental it covers (IS-13) has a deterministic reader, so this is a CANDIDATE for advisory \u2014 but no evidence is recorded that the model's answer fails to reach the customer, and a reader answers the question without silencing the narrative. It keeps blocking until that is measured (R-F4278)"
    },
    "tooluse_trace_unavailable": {
      "axis": "tooluse_trace_unavailable",
      "mechanism": "not_deterministic",
      "fundamentals": {
        "IS-13": "deterministic"
      },
      "advisory_candidate": true,
      "override_evidence": null,
      "why": "every fundamental it covers (IS-13) has a deterministic reader, so this is a CANDIDATE for advisory \u2014 but no evidence is recorded that the model's answer fails to reach the customer, and a reader answers the question without silencing the narrative. It keeps blocking until that is measured (R-F4278)"
    }
  },
  "may_block": [
    "tooluse_adverse",
    "tooluse_challenge",
    "tooluse_challenge_unavailable",
    "tooluse_charges",
    "tooluse_contradiction",
    "tooluse_insolvency",
    "tooluse_multihop",
    "tooluse_news_impact",
    "tooluse_ownership",
    "tooluse_person",
    "tooluse_trace",
    "tooluse_trace_unavailable"
  ],
  "advisory": [
    "tooluse_resolution"
  ],
  "llm_fundamentals": [
    "EI-3",
    "EI-4",
    "FS-9",
    "IS-14",
    "IS-16",
    "LR-19",
    "LR-20",
    "OC-7",
    "OC-8"
  ]
}
